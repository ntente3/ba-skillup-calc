{
  "name": "ba-skillup-calc",
  "version": "1.0.0",
  "type": "module",
  "private": false,
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "parity": "node tests/parity.mjs",
    "check": "npm test && npm run parity"
  },
  "engines": {
    "node": ">=18"
  },
  "description": "Blue Archive skill-up material calculator — no-build, client-side",
  "license": "MIT"
}